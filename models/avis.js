// const pool = require ('./db');


// pool.query("create table avis ( idavis int primary key, clientavis varchar(255));", (error, results) => {
//   if (error) throw error;
//   console.log("table created")
// });